# bash script to periodically move files from a directory (e.g. Downloads) to $HOME/.Trash and then empty it
file locations:
- ~/Library/LaunchAgents/com.user.move_to_trash.plist
- ~/move_to_trash.sh
